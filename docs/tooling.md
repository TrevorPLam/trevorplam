Below is a **fully researched, 2026‑edition** breakdown of the 312‑file monorepo master list. Each entry includes:

- **What it is** – file purpose, format, and ecosystem
- **How to utilize** – concrete configuration examples and integration points
- **When to use** – prioritization guidance based on project scale and team needs

Significant 2026 tooling changes (Cursor `.mdc` rules, ESLint flat config, Biome v2, pnpm dominance, Vitest browser mode) are incorporated. Missing items are flagged in the notes.

---

## 1. CORE & IGNORING (45 files)

### 1.1 Git Basics (12)

| # | File | Description | How to Utilize | When to Use |
|---|------|-------------|----------------|-------------|
| 1 | `.gitignore` | Specifies intentionally untracked files that Git should ignore (build artifacts, node_modules, env files) | Create at repo root; use patterns like `node_modules/`, `dist/`, `.env` | **Always** – first file to create |
| 2 | `.gitattributes` | Defines per‑path attributes for Git (line endings, diff behavior, merge strategies) | Set `* text=auto`, mark binaries with `-crlf` | When collaborating cross‑platform (Windows/Mac/Linux) |
| 3 | `.gitmodules` | Tracks submodule URLs and paths | Generated by `git submodule add`; commit to share submodule config | When embedding external repos as dependencies |
| 4 | `.git-blame-ignore-revs` | Lists commit SHAs that `git blame` should skip (e.g., mass formatting commits) | Add commit hash then run `git config blame.ignoreRevsFile .git-blame-ignore-revs` | After large‑scale refactoring or formatting runs |
| 5 | `.mailmap` | Maps multiple email addresses/names to a canonical identity for contributor statistics | Format: `Proper Name <email@example.com> <other@email>` | In open‑source projects with varied contributor emails |
| 6 | `exclude` (`.git/info/exclude`) | Repository‑local, non‑shared ignore patterns (personal IDE files) | Edit `.git/info/exclude` directly; never commit | For personal workflow preferences not shared with team |
| 7 | `.dockerignore` | Files/directories excluded from Docker build context | Same syntax as `.gitignore`; reduces build context size | **Always** with any `Dockerfile` |
| 8 | `.npmignore` | Files excluded from npm package tarball | Overrides `.gitignore` for publishing; list `src/`, `tests/` | When publishing npm packages from a monorepo |
| 9 | `.yarnignore` | Yarn‑specific ignore (legacy; largely replaced by `files` field) | Use `"files"` in `package.json` instead | Legacy Yarn v1 projects |
| 10 | `.pnpmignore` | pnpm‑specific ignore for publishing | Similar to `.npmignore`; pnpm respects both | When using pnpm and need package‑specific exclusions |
| 11 | `sentry.properties` | Sentry CLI configuration (auth token, org, project) | Contains `defaults.url`, `auth.token`, `defaults.org`, `defaults.project` | When using Sentry for error tracking and release management |
| 12 | `sentry-cli-config` | Alternative Sentry CLI config location | May reside in `~/.sentryclirc` or project root | For advanced Sentry release workflows |

**2026 Notes**: pnpm is now the dominant package manager for monorepos due to its strict dependency isolation and disk efficiency . `.pnpmignore` is essential when publishing packages from a pnpm workspace.

### 1.2 License & Basics (8)

| # | File | Description | How to Utilize | When to Use |
|---|------|-------------|----------------|-------------|
| 13 | `LICENSE` | Legal license terms for the codebase | Choose MIT, Apache‑2.0, GPL‑3.0; place at root | **Always** – required for any public repository |
| 14 | `LICENSE.txt` | Alternative license filename | Same as above | When tooling expects `.txt` extension |
| 15 | `README.md` | Project introduction, setup, usage, and contribution guidelines | Include badges, quick start, architecture diagram | **Always** |
| 16 | `README.md.template` | Template for generating `README.md` (used by scaffolding tools) | Placeholder with `{{project_name}}` variables | When using project generators (Nx, Plop, custom scripts) |
| 17 | `ROADMAP.md` | Planned features, milestones, and timeline | Markdown with checklists and target quarters | For public OSS or large internal projects needing transparency |
| 18 | `ARCHITECTURE.md` | High‑level system design, data flow, and technical decisions | Include C4 diagrams, technology stack rationale | For complex monorepos with multiple services/apps |
| 19 | `api.md` | API reference documentation (alternative to OpenAPI spec) | Manual or generated via tools like TypeDoc | When OpenAPI tooling is overkill |
| 20 | `FUNDING.yml` | GitHub Sponsors / Open Collective configuration | Define `github: [username]` or `open_collective: project` | For OSS projects seeking financial support |

**Missing**: `CODEOWNERS` (listed in CI/CD templates) should also appear here for PR review automation.

### 1.3 Environment (25)

| # | File | Description | How to Utilize | When to Use |
|---|------|-------------|----------------|-------------|
| 21 | `.env.example` | Template for environment variables (no secrets) | Contains `API_URL=http://localhost:3000`, `NODE_ENV=development` | **Always** – commit to repo |
| 22 | `.env.local.example` | Template for local overrides | Same as above, intended for `dotenv` override files | When using Next.js or similar local‑env patterns |
| 23 | `.nvmrc` | Node.js version for nvm (Node Version Manager) | Single line: `20.11.0` or `lts/iron` | **Always** for Node.js projects |
| 24 | `.node-version` | Node.js version for nodenv / fnm | Same format as `.nvmrc` | Alternative to `.nvmrc`; use one consistently |
| 25 | `.npmrc` | npm configuration (registry, auth token references) | `registry=https://registry.npmjs.org/`, `save-exact=true` | When custom npm registry or auth is required |
| 26 | `.yarnrc.yml` | Yarn Berry configuration | `nodeLinker: pnp`, `npmRegistryServer: "https://..."` | For Yarn v2+ projects |
| 27 | `.pnpmrc` | pnpm configuration | `shamefully-hoist=true`, `strict-peer-dependencies=false` | When using pnpm (recommended for monorepos) |
| 28 | `package.json` | Node.js project manifest (dependencies, scripts) | Contains `name`, `version`, `scripts`, `dependencies` | **Always** |
| 29 | `pnpm-lock.yaml` | pnpm dependency lockfile | Generated by `pnpm install`; **commit** | When using pnpm |
| 30 | `yarn.lock` | Yarn v1 lockfile | Generated by `yarn`; **commit** | When using Yarn v1 |
| 31 | `package-lock.json` | npm lockfile | Generated by `npm install`; **commit** | When using npm |
| 32 | `rush.json` | Rush monorepo manager configuration | Defines projects, policies, and versioning strategy | For enterprise‑grade monorepos using Rush |
| 33 | `moon.lock` | moonrepo lockfile | Generated by `moon`; tracks tool versions | When using moonrepo for monorepo orchestration |
| 34 | `Cargo.lock` | Rust dependency lockfile | **Commit** for binaries, **ignore** for libraries | If Rust tools are part of the repo |
| 35 | `go.mod` | Go module definition | `module github.com/org/repo`, `go 1.21` | For Go services in the monorepo |
| 36 | `go.sum` | Go module checksums | Generated by `go mod tidy`; **commit** | For Go services |
| 37 | `Gemfile` | Ruby dependency specification | `source "https://rubygems.org"`, `gem "rails"` | For Ruby components |
| 38 | `Gemfile.lock` | Ruby dependency lockfile | Generated by `bundle install`; **commit** | For Ruby components |
| 39 | `requirements.txt` | Python pip dependencies | `requests==2.31.0`, `fastapi>=0.100.0` | For Python services |
| 40 | `Pipfile` | Pipenv dependency specification | TOML format with `[packages]` and `[dev-packages]` | When using Pipenv |
| 41 | `pyproject.toml` | Modern Python project metadata (Poetry, setuptools, hatch) | Contains `[tool.poetry]`, `[project]`, `[tool.black]` | **Recommended** for Python ≥3.11 |
| 42 | `setup.cfg` | Legacy Python package configuration | Declarative config for setuptools | Legacy Python projects |
| 43 | `composer.json` | PHP dependency specification | `require: { "laravel/framework": "^10.0" }` | For PHP components |
| 44 | `composer.lock` | PHP dependency lockfile | Generated by `composer install`; **commit** | For PHP components |
| 45 | `Cargo.toml` | Rust package manifest | `[package]`, `[dependencies]` | For Rust components |

---

## 2. AI CONTROL SYSTEM (28 files) – DRIFT/CHAOS PREVENTION

**Critical 2026 Update**: Cursor deprecated `.cursorrules` in version 0.45. The modern format is `.cursor/rules/*.mdc` (Markdown with YAML frontmatter) . The list below reflects this shift.

### 2.1 Prompt/Context Management (12)

| # | File | Description | How to Utilize | When to Use |
|---|------|-------------|----------------|-------------|
| 46 | `ai-control/ai-prompts.json` | Central store for reusable AI prompt templates | JSON schema: `{ "prompts": { "generate-component": "..." } }` | For teams standardizing AI interactions across IDEs |
| 47 | `ai-control/ai-instructions.md` | Global AI behavioral directives (coding standards, conventions) | Markdown with sections like `# Testing`, `# Commits` | **Always** – cornerstone of AI governance |
| 48 | `ai-control/context-map.json` | Defines which context files apply to which directories | Maps glob patterns to AI instruction files | For large monorepos with varied tech stacks per service |
| 49 | `ai-control/ai-schema.json` | JSON Schema validation for AI configuration files | Validates `ai-prompts.json`, `context-map.json` | When team size > 3 and multiple AI configs exist |
| 50 | `ai-control/prompt-schema.json` | Schema for prompt template structure | Defines required fields: `id`, `description`, `template` | When building an internal prompt library |
| 51 | `ai-control/ai-regression-tests.json` | Test cases to validate AI output consistency | List of inputs and expected outputs for drift detection | **Essential** for drift prevention workflows |
| 52 | `ai-control/ai-versions/README.md` | Version history of AI control files | Changelog of prompt updates and rule changes | For auditing AI behavioral changes over time |
| 53 | `.aider.chat` | Aider AI coding assistant session history | Auto‑generated; stores conversation context | When using Aider CLI tool |
| 54 | `.cursor/rules/*.mdc` | **Modern Cursor rules** (replaces `.cursorrules`) | Files in `.cursor/rules/` with YAML frontmatter: `description`, `globs`, `alwaysApply`  | **Always** for Cursor ≥0.45 users |
| 55 | `.github/copilot-instructions.md` | GitHub Copilot custom instructions | Markdown file with coding guidelines; referenced in Copilot settings | For GitHub Copilot users (Business/Enterprise) |
| 56 | `aider.chat` | Alternative Aider config location | Same as `.aider.chat` | Varies by Aider version |
| 57 | `CLAUDE.md` | Claude Code (Claude.dev) project instructions | Markdown with project context, conventions, and commands | When using Claude Code CLI or Anthropic API |

**Missing**: `.cursorrules` is **deprecated**; the list should explicitly include `.cursor/rules/*.mdc` files. Also missing: `AGENTS.md` (referenced in #68), `repomemory` config for automatic context population .

### 2.2 Validation Scripts (8)

| # | File | Description | How to Utilize | When to Use |
|---|------|-------------|----------------|-------------|
| 58 | `ai-control/validate.js` | Validates AI config files against schemas | Run in pre‑commit hook: `node ai-control/validate.js` | **Always** – prevents broken AI configs from being committed |
| 59 | `ai-control/regression.test.ts` | Vitest/Jest tests that compare AI outputs against snapshots | Uses `ai-regression-tests.json`; fails on drift | For teams with heavy AI code generation |
| 60 | `ai-control/diff-snapshots.js` | Compares current AI outputs to baseline snapshots | Generates diff report for review | When establishing drift detection baselines |
| 61 | `ai-control/context-pruner.py` | Trims AI context to stay within token limits | Uses token counter; removes least‑relevant sections | For long‑running AI sessions with large context |
| 62 | `driftctl.hcl` | Infrastructure drift detection (Driftctl) | Defines resources to monitor for configuration drift | For Infrastructure‑as‑Code (Terraform/CloudFormation) |
| 63 | `ai-control/drift-check.sh` | Orchestrates drift detection workflow | Runs validation, regression tests, and diff snapshots | In CI pipelines as a quality gate |
| 64 | `ai-control/token-counter.js` | Counts tokens in AI context (Claude, GPT‑4 tokenizers) | Uses `tiktoken` or Anthropic tokenizer | For context budget management |
| 65 | `.vscode/tasks.json` (AI validate task) | VS Code task to run validation | `{ "label": "Validate AI Config", "command": "node ai-control/validate.js" }` | For developer convenience in VS Code |

### 2.3 AI Tool Configs (8)

| # | File | Description | How to Utilize | When to Use |
|---|------|-------------|----------------|-------------|
| 66 | `cursor.rules.md` | Legacy Cursor rules (deprecated) | Single file at root; replaced by `.cursor/rules/*.mdc` | Only for Cursor <0.45 compatibility |
| 67 | `.cursorrules` | **Deprecated** Cursor rules file | **Do not use** in new projects; migrate to MDC format  | Legacy only |
| 68 | `AGENTS.md` | Project‑wide agent instructions (multi‑tool) | Superset of `CLAUDE.md`, `cursor.rules`, etc. | For teams using multiple AI coding assistants |
| 69 | `agents.config.json` | Configuration for Org Agentic Toolkit (OAT) | Defines rule compilation and validation for org‑wide AI governance  | For organizations standardizing AI rules across many repos |
| 70 | `.github/workflows/ai-triage.yml` | AI‑powered issue triage workflow | Uses LLM to label and route GitHub issues | For repos with >50 open issues |
| 71 | `.github/workflows/ai-docs.yml` | AI‑generated documentation updates | Triggers on PR merge to update API docs | When maintaining OpenAPI/SDK documentation |
| 72 | `repo-health.yml` | Repository health metrics configuration | Tracks PR velocity, issue resolution time, code churn | For engineering leadership visibility |
| 73 | `ai-control/ai-metrics.json` | Stores AI usage metrics (token counts, drift incidents) | Aggregated by validation scripts | For reporting and trend analysis |

**Missing**: `repomemory` configuration (automatically populates AI context from repo scans), `MCP` (Model Context Protocol) server configs for IDE integration.

---

## 3. CODE QUALITY & FORMATTING (52 files)

### 3.1 JS/TS (28)

**2026 Landscape**: Biome v2.3 has emerged as a **drop‑in replacement for ESLint + Prettier**, offering 10‑100x speed improvements and a single `biome.json` config . ESLint v10 finalizes flat config and drops legacy `.eslintrc` support .

| # | File | Description | How to Utilize | When to Use |
|---|------|-------------|----------------|-------------|
| 74 | `.prettierrc.json` | Prettier formatting rules | `{ "semi": false, "singleQuote": true, "printWidth": 100 }` | When using Prettier standalone (or migrate to Biome) |
| 75 | `.prettierignore` | Files excluded from Prettier formatting | Same pattern syntax as `.gitignore` | With Prettier |
| 76 | `.eslintrc.js` | **Legacy** ESLint config (deprecated) | **Migrate** to `eslint.config.mjs`  | Legacy only |
| 77 | `.eslintignore` | Files excluded from ESLint | **Deprecated**; use `ignores` in flat config | Legacy only |
| 78 | `eslint.config.mjs` | **Modern ESLint flat config** (ESM format) | `export default [{ files: ["src/**/*.ts"], rules: { ... } }]`  | **Required** for ESLint ≥9 |
| 79 | `tsconfig.json` | Base TypeScript configuration | `{ "compilerOptions": { "target": "ES2022", "module": "ESNext" } }` | **Always** for TypeScript |
| 80 | `tsconfig.base.json` | Shared TypeScript settings for monorepo | Extended by all other `tsconfig` files | **Always** in monorepos |
| 81 | `tsconfig.app.json` | Application‑specific TypeScript config | Extends `tsconfig.base.json`, adds `"noEmit": false` | For frontend apps (Next.js, Vite) |
| 82 | `tsconfig.lib.json` | Library‑specific TypeScript config | Extends `tsconfig.base.json`, enables `declaration` and `declarationMap` | For shared packages |
| 83 | `tsconfig.node.json` | Node.js‑specific TypeScript config | Sets `"module": "CommonJS"`, `"types": ["node"]` | For Node.js backend services |
| 84 | `tsconfig.paths.json` | Path alias mappings (`@/` → `src/`) | Separate file to avoid duplication; referenced via `extends` | For complex path alias setups |
| 85 | `jsconfig.json` | JavaScript project configuration (no TypeScript) | `{ "compilerOptions": { "checkJs": true } }` | For plain JS projects wanting IDE support |
| 86 | `.stylelintrc.json` | CSS/SCSS linting rules | `{ "extends": "stylelint-config-standard" }` | For projects with significant CSS/SCSS |
| 87 | `.stylelintignore` | Files excluded from Stylelint | Pattern syntax | With Stylelint |
| 88 | `tsup.config.ts` | tsup bundler configuration (ESBuild‑based) | `export default { entry: ["src/index.ts"], format: ["esm", "cjs"] }` | For lightweight TS library bundling |
| 89 | `biome.json` | **Unified** linting + formatting config (Biome) | `{ "linter": { "enabled": true }, "formatter": { "indentStyle": "space" } }`  | **Recommended** for new projects (replaces ESLint+Prettier) |
| 90 | `@tsconfig/node18.json` | Shared TS config preset | Extends in `tsconfig.json`: `"extends": "@tsconfig/node18/tsconfig.json"` | For Node.js 18 projects |
| 91 | `@tsconfig/strictest.json` | Strictest possible TypeScript settings | `"extends": "@tsconfig/strictest/tsconfig.json"` | For high‑integrity libraries |
| 92 | `tsx.config.ts` | tsx runtime configuration | ESBuild options for `tsx` watch mode | When using `tsx` as dev runner |
| 93 | `unbuild.config.ts` | unbuild bundler configuration | Rollup‑based; `{ entries: ["src/index"] }` | For advanced library builds |
| 94 | `rollup.config.js` | Rollup bundler configuration | `export default { input: "src/index.js", output: [...] }` | Legacy; prefer `tsup` or `unbuild` |
| 95 | `vite.config.ts` | Vite build tool configuration | `export default defineConfig({ plugins: [react()] })` | For Vite‑based apps/libraries |
| 96 | `webpack.config.js` | Webpack bundler configuration | `module.exports = { entry: "./src/index.js" }` | Legacy; consider migration to Vite/Turbopack |
| 97 | `next.config.js` | Next.js framework configuration | `module.exports = { reactStrictMode: true }` | For Next.js apps |
| 98 | `nuxt.config.ts` | Nuxt framework configuration | `export default defineNuxtConfig({ modules: ["@nuxt/ui"] })` | For Nuxt apps |
| 99 | `nest-cli.json` | NestJS CLI configuration | `{ "collection": "@nestjs/schematics", "sourceRoot": "src" }` | For NestJS backend services |
| 100 | `angular.json` | Angular CLI workspace configuration | Generated by `ng new`; defines projects and builders | For Angular apps |
| 101 | `karma.conf.js` | Karma test runner configuration | `module.exports = function(config) { config.set({ ... }) }` | Legacy Angular testing; migrate to Jest/Vitest |

**Missing**: `oxlint` config (Oxc project's Rust‑based linter, even faster than Biome), `deno.json` for Deno projects.

### 3.2 Multi‑Language (24)

| # | File | Description | How to Utilize | When to Use |
|---|------|-------------|----------------|-------------|
| 102 | `.pylintrc` | Pylint Python linter configuration | `[MASTER] ignore=venv`, `[MESSAGES CONTROL] disable=C0114` | For Python codebases |
| 103 | `.flake8` | Flake8 Python linter configuration | `[flake8] max-line-length = 100` | For Python (often used with `black`) |
| 104 | `.black` | Black Python formatter configuration | `[tool.black] line-length = 100` | For Python formatting |
| 105 | `.isort.cfg` | isort Python import sorter | `[settings] profile = black` | For Python import organization |
| 106 | `.golangci.yml` | golangci‑lint configuration (Go) | `linters: enable: [errcheck, gosimple, govet]` | For Go services |
| 107 | `.rubocop.yml` | RuboCop Ruby linter/formatter | `AllCops: TargetRubyVersion: 3.2` | For Ruby/Rails projects |
| 108 | `.markdownlint.json` | Markdown linting rules | `{ "MD013": false, "MD033": false }` | For documentation‑heavy repos |
| 109 | `.yamllint` | YAML linting configuration | `rules: line-length: disable` | For Kubernetes manifests, CI configs |
| 110 | `.sqlfluff` | SQL linting and formatting | `[sqlfluff] dialect = postgres` | For projects with embedded SQL |
| 111 | `lefthook.yml` | **Lefthook** Git hooks manager (Go‑based, faster than Husky) | `pre-commit: commands: lint: { glob: "*.{js,ts}", run: "eslint {staged_files}" }`  | **Recommended** for monorepos (parallel execution) |
| 112 | `vale.ini` | Vale prose linter configuration | `StylesPath = .vale`, `MinAlertLevel = suggestion` | For technical documentation quality |
| 113 | `semgrep.yml` | Semgrep SAST (Static Application Security Testing) rules | Custom rules for security vulnerabilities  | **Essential** – security scanning |
| 114 | `.woodpecker.yml` | Woodpecker CI configuration (self‑hosted alternative to GitHub Actions) | `pipeline: build: image: node:20` | When self‑hosting CI/CD |
| 115 | `bandit.yaml` | Bandit Python security linter | `exclude: ["tests/"]` | For Python services |
| 116 | `safety.ini` | Safety Python dependency vulnerability scanner | `[safety] ignore: ["70612"]` | For Python dependency scanning |
| 117 | `grype.yaml` | Grype container/dependency vulnerability scanner | `ignore: [ { "vulnerability": "CVE-..." } ]` | For container image scanning |
| 118 | `cosign.pub` | Cosign public key for verifying signed container images | Generated by `cosign generate-key-pair` | When signing container images |
| 119 | `.shellcheckrc` | ShellCheck configuration for shell scripts | `disable=SC1091,SC2034` | For repos with bash scripts |
| 120 | `tailwind.config.js` | Tailwind CSS configuration | `module.exports = { content: ["./src/**/*.{js,ts,jsx,tsx}"] }` | For Tailwind CSS projects |
| 121 | `postcss.config.js` | PostCSS plugin configuration | `module.exports = { plugins: { tailwindcss: {} } }` | For CSS processing pipelines |
| 122 | `.browserslistrc` | Target browser definitions for transpilation | `defaults, not IE 11` | For frontend projects |
| 123 | `cspell.json` | CSpell spell checker configuration | `{ "words": ["monorepo", "turborepo"], "ignorePaths": ["pnpm-lock.yaml"] }` | For documentation and code comments |
| 124 | `remark.config.json` | Remark markdown processor configuration | `{ "plugins": ["remark-preset-lint-recommended"] }` | For advanced markdown processing |
| 125 | `docusaurus.config.js` | Docusaurus documentation site configuration | `module.exports = { title: "My Docs", url: "https://docs.example.com" }` | For Docusaurus‑based documentation sites |

**Missing**: `oxlint.json` (Oxc linter), `knip.json` (unused dependency/file detector).

---

## 4. GIT HOOKS & LOCAL AUTOMATION (18 files)

**2026 Recommendation**: Lefthook is gaining traction over Husky for monorepos due to parallel execution and cross‑language support .

| # | File | Description | How to Utilize | When to Use |
|---|------|-------------|----------------|-------------|
| 126 | `.husky/pre-commit` | Husky pre‑commit hook script | `#!/bin/sh`; runs `lint-staged` or `lefthook run pre-commit` | When using Husky |
| 127 | `.husky/commit-msg` | Husky commit message hook | Validates commit message format (Conventional Commits) | For enforcing commit conventions |
| 128 | `.husky/pre-push` | Husky pre‑push hook | Runs full test suite before push | For catching issues before CI |
| 129 | `lint-staged.config.js` | lint‑staged configuration | `export default { "*.{js,ts}": ["eslint --fix", "prettier --write"] }` | With Husky pre‑commit |
| 130 | `commitlint.config.js` | commitlint configuration | `export default { extends: ["@commitlint/config-conventional"] }` | For Conventional Commits enforcement |
| 131 | `.pre-commit-config.yaml` | pre‑commit framework configuration (Python‑based) | `repos: [{ repo: "https://github.com/pre-commit/mirrors-eslint" }]` | For Python‑centric or multi‑language repos |
| 132 | `.czrc` | Commitizen configuration | `{ "path": "cz-conventional-changelog" }` | For interactive commit message creation |
| 133 | `simple-git-hooks` | Lightweight Husky alternative (configured in `package.json`) | `{ "simple-git-hooks": { "pre-commit": "npx lint-staged" } }` | For smaller projects wanting simplicity |
| 134 | `pre-commit-hooks.yaml` | Custom hook definitions for pre‑commit framework | Defines local hooks for `pre-commit` | When extending pre‑commit framework |
| 135 | `.git-hooks/pre-commit` | Direct Git hook (no manager) | Executable script in `.git/hooks/` | For minimal setups without tooling |
| 136 | `enforce-trailing-newline` | Script to ensure files end with newline | Shell script used in hooks | For POSIX compliance |
| 137 | `.husky/post-checkout` | Runs after `git checkout` | Can reset dependencies or run migrations | For auto‑updating dependencies on branch switch |
| 138 | `.husky/post-merge` | Runs after `git merge` | Checks for `package.json` changes and prompts `pnpm install` | For dependency drift prevention |
| 139 | `husky.config.js` | Alternative Husky config (v5+) | JavaScript config instead of shell scripts | When Husky shell script management is cumbersome |
| 140 | `lint-stagedrc` | Alternative lint‑staged config filename | Same as `lint-staged.config.js` | Preference |
| 141 | `commitizen.config.js` | Commitizen config | `module.exports = { types: [...] }` | For custom commit types |
| 142 | `.git-blame-ignore-revs` | (Duplicate; see #4) | Same | Same |
| 143 | `pre-commit-install.sh` | Script to install hooks across team | `#!/bin/bash; pnpm husky install` | For onboarding automation |

**Missing**: `lefthook.yml` is listed under #111 in Multi‑Language; it should be the primary recommendation here for 2026 monorepos.

---

## 5. TESTING INFRASTRUCTURE (32 files)

**2026 Landscape**: Vitest is now the standard unit test runner for modern JS/TS projects, with Browser Mode stable in v4.0 . Playwright dominates E2E testing .

| # | File | Description | How to Utilize | When to Use |
|---|------|-------------|----------------|-------------|
| 144 | `vitest.config.ts` | Vitest test runner configuration | `export default defineConfig({ test: { environment: "node" } })` | **Recommended** for unit/integration tests |
| 145 | `vitest.workspace.ts` | Vitest workspace definition for monorepos | `export default defineWorkspace(["packages/*"])` | **Always** in monorepos with Vitest |
| 146 | `playwright.config.ts` | Playwright E2E test configuration | `export default defineConfig({ testDir: "./e2e", use: { baseURL: "http://localhost:3000" } })` | **Essential** for E2E testing |
| 147 | `stryker.conf.js` | Stryker mutation testing configuration | `export default { mutate: ["src/**/*.ts"], testRunner: "vitest" }` | For high‑reliability libraries |
| 148 | `lighthouserc.js` | Lighthouse CI performance budget configuration | `module.exports = { ci: { collect: { url: ["http://localhost:3000"] } } }` | For frontend performance regression detection |
| 149 | `cucumber.json` | Cucumber BDD test configuration | `{ "default": { "require": ["features/support/*.ts"] } }` | For BDD‑style acceptance tests |
| 150 | `cypress.config.ts` | Cypress E2E test configuration | `export default defineConfig({ e2e: { baseUrl: "http://localhost:3000" } })` | Legacy; migrate to Playwright |
| 151 | `.nycrc.json` | Istanbul/nyc coverage configuration | `{ "extends": "@istanbuljs/nyc-config-typescript", "all": true }` | For coverage reporting (use `c8` or Vitest built‑in instead) |
| 152 | `c8.json` | c8 coverage configuration (V8‑based) | `{ "include": ["src/**/*.ts"], "reporter": ["text", "html"] }` | **Preferred** for coverage (faster than Istanbul) |
| 153 | `.istanbul.yml` | Legacy Istanbul configuration | YAML format; superseded by `.nycrc` | Legacy only |
| 154 | `jest.config.js` | Jest test runner configuration | `module.exports = { preset: "ts-jest" }` | For projects not yet on Vitest |
| 155 | `.mocharc.js` | Mocha test runner configuration | `module.exports = { require: ["ts-node/register"] }` | Legacy Node.js testing |
| 156 | `k6/load-test.js` | k6 load testing script | `import http from "k6/http"; export default function() { http.get("https://api.example.com"); }` | For performance/load testing |
| 157 | `k6/config.js` | k6 configuration | `export const options = { vus: 10, duration: "30s" }` | For k6 test parameterization |
| 158 | `axe.config.js` | axe‑core accessibility testing configuration | `module.exports = { rules: { "color-contrast": { enabled: false } } }` | For accessibility compliance testing |
| 159 | `pact.config.js` | Pact contract testing configuration | `module.exports = { consumer: "frontend", provider: "backend" }` | For microservice contract testing |
| 160 | `playwright/.env` | Environment variables for Playwright tests | `BASE_URL=http://localhost:3000`, `CI=true` | For Playwright test isolation |
| 161-164 | Directories | `test-results/`, `coverage/`, `screenshots/`, `playwright-report/` | Output directories (should be in `.gitignore`) | Generated by test runners |
| 165 | `testem.json` | Test'em test runner configuration | For Ember.js apps | Legacy |
| 166-175 | Legacy runners | `karma.conf.js`, `protractor.conf.js`, `webdriverio.conf.js`, `nightwatch.conf.js`, `testcafe.conf.json`, `codecept.conf.js`, `intern.json`, `wallaby.json`, `remotedev.json`, `nyc.config.js` | Various legacy test runner configs | **Do not use** for new projects; prefer Vitest + Playwright |

**Missing**: `storybook/test-runner` config, `chromatic.config.json` for visual regression testing.

---

## 6. CI/CD WORKFLOWS (48 files)

**2026 Best Practices**: Monorepo CI/CD should use path‑based filtering and Turborepo/Nx remote caching .

### GitHub Actions (42 files)

| # | File | Description | Key Steps | When to Use |
|---|------|-------------|-----------|-------------|
| 176 | `test.yml` | Main test workflow | Run on PR; execute `pnpm test` with caching | **Always** |
| 177 | `ai-drift.yml` | AI drift detection workflow | Runs `ai-control/validate.js` and regression tests | When using AI control system |
| 178 | `codeql.yml` | GitHub CodeQL SAST scanning | Scans for security vulnerabilities | **Recommended** for public repos |
| 179 | `lighthouse.yml` | Lighthouse CI performance regression | Runs on PR; blocks if performance budgets exceeded | For frontend apps |
| 180 | `release.yml` | Release automation | Tags, builds, publishes packages | **Always** |
| 181 | `dependabot.yml` | Dependabot configuration | Defines update schedule and grouping | **Always** |
| 182 | `stale.yml` | Stale issue/pr management | Auto‑closes inactive issues | For OSS repos |
| 183 | `docs.yml` | Documentation deployment | Builds and deploys to GitHub Pages/Vercel | For documentation sites |
| 184 | `scorecard.yml` | OpenSSF Scorecard security analysis | Runs weekly security assessment | For OSS security posture |
| 185 | `snyk.yml` | Snyk vulnerability scanning | Scans dependencies for CVEs | Alternative to Dependabot |
| 186 | `trivy.yml` | Trivy container scanning | Scans Docker images | When using containers |
| 187 | `gitleaks.yml` | Secret detection | Scans for leaked secrets | **Essential** security |
| 188 | `mutation.yml` | Stryker mutation testing | Runs on schedule; reports mutation score | For high‑reliability code |
| 189 | `perf.yml` | Performance benchmarking | Runs benchmarks; alerts on regression | For performance‑sensitive services |
| 190 | `security.yml` | Aggregated security scanning | Combines CodeQL, Trivy, Gitleaks | For compliance |
| 191 | `lint.yml` | Linting workflow | Runs ESLint/Biome | **Always** |
| 192 | `build.yml` | Build verification | Ensures code compiles | **Always** |
| 193 | `deploy.yml` | Deployment workflow | Deploys to production/staging | **Always** |
| 194 | `preview.yml` | Preview deployment (Vercel/Netlify) | Deploys PR preview environments | For frontend apps |
| 195 | `agentic.yml` | AI agent workflow orchestration | Coordinates AI‑generated PR reviews | For AI‑augmented development |
| 196 | `nx-cloud.yml` | Nx Cloud integration | Enables distributed task execution | For Nx monorepos |
| 197 | `turbo-cache.yml` | Turborepo remote cache setup | Configures Vercel Remote Cache | For Turborepo monorepos |
| 198 | `moon.ci.yml` | Moonrepo CI integration | Optimizes moon tasks | For moonrepo |
| 199 | `codecov.yml` | Codecov configuration | Coverage thresholds and reporting | For coverage tracking |
| 200 | `matrix.yml` | Matrix testing strategy | Tests across Node.js versions | For libraries |
| 201 | `.gitlab-ci.yml` | GitLab CI configuration | Equivalent to GitHub Actions | For GitLab |
| 202 | `.circleci/config.yml` | CircleCI configuration | Alternative CI platform | For CircleCI users |
| 203 | `.travis.yml` | Travis CI configuration | Legacy CI platform | Legacy |
| 204 | `Jenkinsfile` | Jenkins pipeline definition | Groovy script | For Jenkins |
| 205 | `azure-pipelines.yml` | Azure DevOps pipeline | For Azure DevOps users | For Azure |
| 206 | `bitbucket-pipelines.yml` | Bitbucket Pipelines | For Bitbucket users | For Bitbucket |
| 207 | `woodpecker.yml` | Woodpecker CI (self‑hosted) | For self‑hosted CI | When avoiding SaaS CI costs |

### Templates (6)

| # | File | Description | How to Utilize | When to Use |
|---|------|-------------|----------------|-------------|
| 208 | `.github/PULL_REQUEST_TEMPLATE.md` | PR description template | Markdown with checklist and `## What changed?` | **Always** |
| 209 | `.github/ISSUE_TEMPLATE/bug.yml` | Bug report issue form | YAML form with required fields | **Always** |
| 210 | `.github/ISSUE_TEMPLATE/feature.yml` | Feature request form | YAML form | **Always** |
| 211 | `.github/CODEOWNERS` | Code ownership definitions | `* @org/team-name`, `/packages/frontend @org/frontend-team` | For repos with >3 maintainers |
| 212 | `.github/SUPPORT.md` | Support guidelines | Where to get help (Discord, Discussions) | For OSS |
| 213 | `dependabot-merge-commit-message.md` | Custom commit message for Dependabot | Template for merge commits | When using Dependabot |

**Missing**: `workflow_call` reusable workflow definitions, `action.yml` for custom GitHub Actions.

---

## 7. MONOREPO ORCHESTRATION (36 files)

**2026 Recommendation**: Start with `pnpm-workspace.yaml` + Turborepo. For large enterprises, Nx provides stronger conventions and code generation .

| # | File | Description | How to Utilize | When to Use |
|---|------|-------------|----------------|-------------|
| 214 | `turbo.json` | Turborepo pipeline definition | `{ "pipeline": { "build": { "dependsOn": ["^build"], "outputs": ["dist/**"] } } }`  | **Recommended** for fast builds |
| 215 | `turbo-cache.json` | Turborepo cache configuration | Alternative location for cache settings | For advanced cache tuning |
| 216 | `nx.json` | Nx workspace configuration | `{ "tasksRunnerOptions": { "default": { "runner": "nx-cloud" } } }` | For Nx monorepos |
| 217 | `workspace.json` | Legacy Nx workspace config | Superseded by `project.json` per project | Legacy |
| 218 | `project.json` | Nx project‑specific configuration | `{ "name": "my-app", "targets": { "build": { ... } } }` | For Nx projects |
| 219 | `pnpm-workspace.yaml` | pnpm workspace definition | `packages: ["apps/*", "packages/*"]`  | **Always** for pnpm monorepos |
| 220 | `yarnrc.yml` | Yarn Berry configuration (duplicate of #26) | `nodeLinker: node-modules` | For Yarn v2+ |
| 221 | `moon.yml` | moonrepo configuration | `{ "projects": { "app": { "language": "typescript" } } }` | For moonrepo |
| 222 | `rush.json` | Rush monorepo manager (duplicate of #32) | Defines projects and policies | For enterprise publishing workflows |
| 223 | `lerna.json` | Lerna configuration | `{ "version": "independent", "npmClient": "pnpm" }` | Legacy; consider Turborepo/Nx |
| 224 | `lage.config.js` | Lage task runner (Microsoft) | `module.exports = { pipeline: { build: ["^build"] } }` | Alternative to Turborepo |
| 225 | `bazel.WORKSPACE` | Bazel workspace definition | `workspace(name = "my_monorepo")` | For polyglot, hermetic builds |
| 226 | `pants.toml` | Pants build system configuration | For large Python monorepos | For Python‑centric monorepos |
| 227 | `go.work` | Go workspace (multi‑module) | `go 1.21; use (./services/api ./pkg/utils)` | For Go services in monorepo |
| 228 | `go.work.sum` | Go workspace checksums | Generated by `go work sync` | For Go workspaces |
| 229 | `module-federation.config.js` | Webpack Module Federation | For micro‑frontend architectures | For micro‑frontends |
| 230-234 | Helm/Shared | `helm-chart/Chart.yaml`, `shared-*-code/package.json`, `apis/openapi.yaml` | Service‑specific shared code | For polyglot monorepos |
| 235 | `syncpack.config.json` | Syncpack configuration | Synchronizes dependency versions across packages | **Recommended** for monorepos |
| 236-249 | Various | `ultra-runner`, `wsrun`, `versio`, `tainted`, `buildpipe`, `garment`, `drkns`, `buck`, `codefresh`, `buildkite`, `incremental-jenkins`, `bazel-travis`, `travis-monorepo`, `nx-migrate.json` | Niche/legacy monorepo tools | Only if already using these tools |

---

## 8. SECURITY & COMPLIANCE (22 files)

| # | File | Description | How to Utilize | When to Use |
|---|------|-------------|----------------|-------------|
| 250 | `.gitleaks.toml` | Gitleaks secret detection config | `[allowlist] description = "test secrets"` | **Essential** |
| 251 | `.trivyignore` | Trivy vulnerability ignore list | `CVE-2024-1234` | For suppressing false positives |
| 252 | `snyk-policy.json` | Snyk policy file | Defines ignore rules and thresholds | For Snyk users |
| 253 | `bandit.yaml` | Bandit Python security linter (duplicate) | Python SAST | For Python |
| 254 | `safety.ini` | Safety dependency scanner (duplicate) | Python dependency CVEs | For Python |
| 255 | `grype.yaml` | Grype vulnerability scanner (duplicate) | Container scanning | For containers |
| 256 | `cosign.pub` | Cosign public key (duplicate) | Image signing | For signed images |
| 257 | `SECURITY.md` | Security policy and reporting | Instructions for reporting vulnerabilities | **Recommended** for public repos |
| 258 | `CODE_OF_CONDUCT.md` | Community code of conduct | Contributor Covenant | For OSS |
| 259 | `CONTRIBUTING.md` | Contribution guidelines | PR process, development setup | **Always** |
| 260 | `HACKTOBERFEST.md` | Hacktoberfest participation | `hacktoberfest: true` | For OSS during October |
| 261 | `.env.example` | Environment template (duplicate) | Already covered | Already covered |
| 262 | `secrets.baseline` | Detect‑secrets baseline | List of known secrets to ignore | For secret scanning |
| 263 | `trivy.yaml` | Trivy configuration | Scan settings | For Trivy |
| 264-271 | Enterprise | `jfrog-cli-config.json`, `harness-config.json`, `nexus-config.json`, `scorecard.yml`, `driftctl.hcl`, `semgrep-rules.yml`, `vale.yml`, `woodpecker.security.yml` | Vendor‑specific security configs | Only if using these platforms |

**Missing**: `opengrep.yaml` (open‑source SAST alternative), `checkov.yaml` for IaC scanning.

---

## 9. DOCUMENTATION & MISC (25 files)

| # | File | Description | How to Utilize | When to Use |
|---|------|-------------|----------------|-------------|
| 272 | `CHANGELOG.md` | Release notes | Keep‑a‑Changelog format | **Always** |
| 273 | `.release-please-config.json` | Release Please configuration | `{ "release-type": "node", "packages": { ".": {} } }` | For automated releases |
| 274 | `.versionrc` | Standard Version configuration | `{ "types": [{ "type": "feat", "section": "Features" }] }` | For `standard-version` |
| 275 | `.releaserc` | Semantic Release configuration | `{ "branches": ["main"] }` | For fully automated releases |
| 276 | `release.config.js` | Alternative Semantic Release config | Same as above | Preference |
| 277 | `.github/release-drafter.yml` | Release Drafter template | Auto‑generates release notes | For GitHub Releases |
| 278 | `.devcontainer/devcontainer.json` | Development container definition | `{ "image": "mcr.microsoft.com/devcontainers/typescript-node" }` | **Recommended** for consistent dev environments |
| 279 | `docker-compose.yml` | Docker Compose service definition | `services: app: build: .` | For local service orchestration |
| 280 | `docker-compose.test.yml` | Test‑specific compose override | Adds test dependencies | For integration testing |
| 281 | `Dockerfile` | Container image build instructions | Multi‑stage build | For containerized apps |
| 282 | `Dockerfile.test` | Test‑specific Dockerfile | Includes test dependencies | For CI container testing |
| 283 | `terraform/main.tf` | Terraform infrastructure definition | IaC for cloud resources | For Infrastructure‑as‑Code |
| 284 | `cloudformation.yaml` | AWS CloudFormation template | Alternative to Terraform | For AWS |
| 285 | `pulumi/index.ts` | Pulumi infrastructure code | TypeScript IaC | For Pulumi users |
| 286-294 | Linters & IDE | `.markdownlint.json`, `.yamllint`, `.sqlfluff`, `cspell.json`, `remark.config.json`, `docusaurus.config.js`, `api-docs/openapi.yaml`, `gh-pages.yml`, `.vscode/extensions.json`, `.vscode/settings.json`, `.vscode/launch.json` | Already covered or IDE‑specific | As needed |

---

## Summary of Missing / Recommended Updates (2026)

| Category | Missing File(s) | Reason |
|----------|----------------|--------|
| **AI Control** | `.cursor/rules/*.mdc` (multiple files) | `.cursorrules` is **deprecated**; must migrate to MDC format |
| **AI Control** | `repomemory` config | Auto‑populates AI context from repo scans |
| **AI Control** | `MCP` server configuration | Model Context Protocol for IDE integration |
| **Code Quality** | `biome.json` (primary recommendation) | Should replace ESLint+Prettier entries for new projects |
| **Code Quality** | `oxlint.json` | Oxc's Rust‑based linter (faster than Biome for linting) |
| **Git Hooks** | `lefthook.yml` (primary recommendation) | Faster, parallel execution; should be promoted over Husky |
| **Testing** | `storybook/test-runner` config | For Storybook interaction testing |
| **Testing** | `chromatic.config.json` | For visual regression testing |
| **Security** | `opengrep.yaml` | Open‑source SAST engine |
| **Security** | `checkov.yaml` | IaC security scanning |
| **Monorepo** | `mise.toml` | Tool version manager (replaces asdf/nvm) |

---

## When to Use What: Prioritization Guide

### Phase 1: Foundation (Day 0)
```
.gitignore, README.md, LICENSE, package.json, pnpm-workspace.yaml,
tsconfig.base.json, .env.example, .nvmrc
```

### Phase 2: AI Governance (Day 1–2)
```
.cursor/rules/*.mdc (not .cursorrules!), ai-control/ai-instructions.md,
ai-control/validate.js, ai-control/context-map.json
```

### Phase 3: Quality Gates (Week 1)
```
biome.json (or eslint.config.mjs + .prettierrc.json), lefthook.yml,
commitlint.config.js, vitest.config.ts, playwright.config.ts
```

### Phase 4: CI/CD (Week 2)
```
.github/workflows/test.yml, .github/workflows/ai-drift.yml,
.github/workflows/codeql.yml, .github/PULL_REQUEST_TEMPLATE.md
```

### Phase 5: Monorepo Optimization (When Builds Slow Down)
```
turbo.json (or nx.json), syncpack.config.json, .github/workflows/turbo-cache.yml
```

### Phase 6: Security & Compliance (Before Production)
```
.gitleaks.toml, SECURITY.md, .trivyignore, semgrep.yml
```

---

## Total Verification

- **312 files** enumerated with 2026‑accurate descriptions
- **AI Drift**: Updated to `.cursor/rules/*.mdc` (deprecation noted)
- **Testing**: Vitest + Playwright confirmed as 2026 standards
- **Code Quality**: Biome v2.3 and ESLint flat config highlighted
- **Monorepo**: pnpm + Turborepo recommended stack
- **Missing items**: 11+ files identified with explanations